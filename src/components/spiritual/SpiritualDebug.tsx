// SpiritualDebug.tsx - this debug component has been removed.
// It relied on legacy backend-specific RPC calls which no longer exist.
// Debug the spiritual feature via the /health endpoint or normal API calls instead.

export default function SpiritualDebug() {
  return null;
}

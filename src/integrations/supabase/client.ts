// Supabase has been removed. This app now uses Better Auth + Hono.
// This stub exists only to satisfy any stale imports that haven't been cleaned up yet.
// DO NOT add new code that imports from here — use @/lib/api.ts instead.

export const supabase = null;